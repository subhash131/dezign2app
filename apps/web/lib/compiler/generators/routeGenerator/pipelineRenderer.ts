/**
 * Pipeline Renderer
 * Modular pipeline compiler system for API endpoints, consumers, and event pipelines.
 * Re-exports all components from the modular `./pipeline` directory for backward compatibility.
 */
export * from "./pipeline";
